# Verification log — Living Node Swarm v0.1

**Date:** 2026-07-27  
**Plan:** `docs/plans/2026-07-27-living-node-swarm-v0.1.md`  
**User overrides applied:** OpenRouter (user-selected models); transform strategies experimental (not locked to affine only)

## Automated tests

### Kernel

```text
cd packages/lns_kernel && PYTHONPATH=src pytest -v
14 passed in 0.07s
```

Coverage: validation, cycles, downstream, reproducible MC, proposed-node exclusion, parent edit changes child quantiles, transform experiment strategies, store patch + resim, freshness after run.

### Server

```text
cd packages/lns_server && PYTHONPATH=src:../lns_kernel/src pytest -v
4 passed in 0.23s
```

Coverage: health bind metadata, seed create → patch → outcome quantile change, transform experiment recommendation, AI endpoint fails without real OpenRouter key/model (no mock success path).

## Manual E2E

| Step | Result |
|------|--------|
| Start API `./scripts/run_local.sh` | **PASS** (user 2026-07-27) |
| Start UI `./scripts/run_ui.sh` | **PASS** (user screenshot + walkthrough) |
| Load seed (3 nodes) | **PASS** |
| Edit input_signal mu | **PASS** — distributions update |
| Freshness honest | **PASS** — fresh badge observed |
| OpenRouter propose | **PASS** — all 3 models configured; propose worked |
| Activate / reject proposed | **Shipped** after user validation; restart API/UI to use |

## Gaps / action plan

| Gap | Action |
|-----|--------|
| Manual browser E2E not executed in this agent session | Run `./scripts/run_local.sh` + `./scripts/run_ui.sh` and walk acceptance path |
| Live OpenRouter call not exercised without user key | User sets `OPENROUTER_API_KEY` + model id and clicks Propose node |
| Transform “best” is heuristic recommendation only | Inspect experiment panel; pick strategy per node intentionally |

## Verdict

- **Automated:** PASS (18/18)  
- **Product shell code:** present for S1–S4 path + OpenRouter + transform experiments  
- **Not claimed:** production-ready, multi-user, or complete until manual E2E + optional live OpenRouter smoke by user  
