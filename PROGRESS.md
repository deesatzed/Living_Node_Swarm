# PROGRESS.md

## Status Overview

**Gate 4 in progress – Gates 0–3 complete; Prediction Workspace is under active verification**

The domain-general contracts, scientific kernel, safe-research primitives, and
fixture-backed generalized authoring API are verified through Gate 3. The
existing v0.1/v0.2 UI shell remains the visual baseline; the generalized
Prediction Workspace has not been built yet.

## Current Baseline

| Surface | Verified state |
|---|---|
| Kernel tests | 72 passed on 2026-07-27 after persisted multi-seed/multi-sample stability diagnostics |
| Server tests | 55 passed on 2026-07-28 after workspace-persistence coverage; one upstream deprecation warning |
| General UI | Production build passed on 2026-07-28 after consuming `@lns/ui-shared` |
| Gas UI | Production build passed on 2026-07-28 after consuming `@lns/ui-shared` |
| Current families | Eight canonical registry families; legacy node payloads remain readable through explicit parameter normalization |
| Current graph | Static DAG with affine/sum/mean transforms |
| Current research/provenance | Local security, provenance, consent, and completeness primitives implemented; no live research/API journey yet |
| Current generalized authoring UX | Both apps consume the shared lifecycle package; typed client/catalog and deterministic fixture groundwork exist, but workspace journeys are not yet implemented |

## Current Assumptions

1. The user wants an ambitious, transparent product rather than a narrow gas demo.
2. Human approval remains mandatory before AI-proposed structure becomes active.
3. The initial eight-family registry is fixed for the first generalized release.
4. Time-expanded DAGs are sufficient for the first delayed-effect implementation.
5. Shared latent parents plus warnings are sufficient for first-release dependence handling.
6. Neodymium history may be difficult to obtain; inability to verify it blocks lift claims, not honest workflow development.
7. No live trading is needed or authorized for this goal.

## Task Tracker

| Task | Status | Owner | Notes |
|---|---|---|---|
| Harden `GOAL.md` | Complete | Codex | Added contracts, gates, proof, safety, UX, stop/complete rules |
| Create project standards | Complete | Codex | `STANDARDS.md` |
| Record architecture/product decisions | Complete | Codex | `DECISIONS.md` |
| Create phased implementation guidance | Complete | Codex | `IMPLEMENT.md` |
| Create actionable queue | Complete | Codex | `TASK_QUEUE.md` |
| Create detailed design and implementation plans | Complete | Codex | Saved under `docs/plans/` |
| Replace stale latest handoff | Complete | Codex | Active packet is `HANDOFF_2026-07-27_DOMAIN_GENERAL.md` |
| Q0-01 `TargetContract` | Complete | Codex | Immutable resolution-grade contract; 5 new tests, 25 kernel tests passed |
| Q0-02 through Q0-05 contract slices | Complete | Codex | Distribution, evidence/source/conflict, relationship, proposal/approval/run contracts; 34 kernel tests passed |
| Gate 0 compatibility and API schemas | Complete | Codex | Legacy graph defaults, public JSON-schema export, and canonical Neodymium contract doc; 37 kernel tests passed |
| Q1-01 distribution registry | Complete | Codex | Eight canonical families, support/parameter metadata, aliases; 42 kernel tests passed |
| Q1-02 validation/statistics | Complete | Codex | Registry-backed finite/parameter/support validation and explicit analytic statistics; 48 kernel tests passed |
| Q1-03 seeded sampling | Complete | Codex | Reproducible support-checked sampling for every canonical family; the runtime Monte Carlo ensemble now delegates to that canonical registry and has an all-eight-family execution test (14 focused kernel tests, 2026-07-28). |
| Weighted mixture primitive | In progress | Codex | Kernel validates exact finite non-negative member weights, normalizes them, and creates a seeded mixture from member outcome samples rather than averaging point summaries. The server accepts 2–8 explicit graph/version/target/weight members, verifies every selected version, returns each member receipt plus the mixture distribution, and labels the result non-mutating/non-recommendatory. A project-scoped typed ensemble configuration persists exact unique graph/version/target bindings and positive-total weights across restart, validating all members against current graphs while preserving the active project graph/version. An ensemble-specific approval endpoint checks the exact saved configuration SHA-256 hash and every member version again, saves a named operator receipt, and proves it did not mutate a graph. Its read-only receipt list survives refresh; it does not activate an ensemble or alter a graph. Run exposes the server-issued saved hash only through the saved configuration, requires named operator plus explicit review acknowledgment, submits that exact binding, renders the returned receipt, and shows persisted receipt history. It can save/list/restore a compatible two-model configuration and provides backend mixture/member receipts and limitations without recommendation language (7 focused kernel ensemble tests; 11 focused workspace tests; 67 server tests; 91 shared tests; 6 fixture E2E tests, 2026-07-28). |
| Q1-04 parameter compatibility | Complete | Codex | Legacy node payloads round-trip; runtime normalizes documented legacy names to frozen registry parameterization; 60 kernel tests passed |
| Q1-05 relationship units | Complete | Codex | Affine coefficient dimensions and aggregate unit compatibility reject invalid graph edges; 63 kernel tests passed |
| Q1-06 time-expanded lags | Complete | Codex | Explicit lag clock, delayed-feedback expansion, and same-time cycle rejection; 66 kernel tests passed |
| Q1-07 dependence warnings | Complete | Codex | Warns on unresolved proxy correlation, duplicate links, and shared-evidence double counting; 69 kernel tests passed |
| Q1-09 continuous scoring | Complete | Codex | Empirical CRPS and interval coverage with reference/invalid-input tests; 71 kernel tests passed |
| Q1-08 stability diagnostics | Complete | Codex | Every successful snapshot persists a bounded multi-seed/multi-sample metric-range receipt and limitation; 72 kernel tests passed |
| Gate 1 scientific kernel | Complete | Codex | Registry, compatibility, units, temporal DAG, dependence warnings, stability receipts, and scoring verified |
| Q2-01 URL safety | Complete | Codex | HTTP(S)-only, credential-free, public-DNS destination validation; 24 server tests passed |
| Q2-02 bounded fetcher | Complete | Codex | Manual redirect revalidation, timeout, streamed byte cap, content-type allowlist; 28 server tests passed |
| Q2-03 evidence persistence | Complete | Codex | Local SQLite stores typed source receipts/claims, hashes, conflicts, and source references across restart; 30 server tests passed |
| Q2-04 untrusted extraction | Complete | Codex | Fixed trusted extraction task, bounded visible text, script/style exclusion, content hash; 32 server tests passed |
| Q2-05 routing receipt | Complete | Codex | Preview exposes provider/model/scope/hashes without source text; confirmed receipt persists; 34 server tests passed |
| Q2-06 research completeness | Complete | Codex | Bounded source/byte/time plan records diversity, contradiction search, saturation, and gaps; 36 server tests passed |
| Q2-07 security/provenance suite | Complete | Codex | Fixture-only end-to-end security/receipt journey, including unsafe redirect rejection; 38 server tests passed |
| Gate 2 safe research/provenance | Complete | Codex | Local safe retrieval, untrusted isolation, consent receipts, persistence, and completeness reporting verified; no live research claimed |
| Q3-01 target intake API | Complete | Codex | Resolution-grade `TargetContract` persists/retrieves; ambiguous target data rejects; 40 server tests passed |
| Q3-02 research-review API | Complete | Codex | Claims show source/conflict/classification and require explicit per-target include/exclude review; 41 server tests passed |
| Q3-03 candidate graph fixture | Complete | Codex | Explicitly labeled deterministic 15-factor, three-hop, proposed-only graph fixture; active graph untouched; 42 server tests passed |
| Q3-04 distribution elicitation | Complete | Codex | Median/P90 quantiles transparently derive Normal or LogNormal parameters, statistics, and a limitation-bearing receipt; 44 server tests passed |
| Eight-family intuitive derivation groundwork | In progress | Codex | A server-owned `/authoring/distributions/derive` route maps all eight families from named human inputs to registry-validated canonical parameters and derived statistics: median/P90 for Normal/LogNormal; mean/concentration for Beta; expected count for Poisson; expected count/dispersion for NegativeBinomial; mean/standard deviation for Gamma; location/scale/degrees of freedom for StudentT; and value for Deterministic. The Edit candidate surface forwards this client and renders family-aware named inputs for the six non-quantile families; it accepts only a server-returned same-family spec and stages matching runtime overrides with provenance. Its exact candidate-review binding includes the full specification; approval atomically writes both the active parameters and the node's provenance, then refreshes inspector as-of/support/method/confidence/evidence fields from the returned graph. The same read-only registry route returns labeled fixed-seed sampled p05/p50/p95 display quantiles for all eight families; they are not fitting, confidence intervals, or accuracy evidence. Component coverage verifies each non-quantile form exposes only its intended human-facing numeric inputs. The fixture Monitor → Edit browser flow sends every non-quantile form through the derivation endpoint and proves Normal/Gamma proposal-review paths; curve editing and fitting remain (86 kernel tests; 89 server tests; 121 shared tests; 8 fixture E2E tests; shared/canonical UI builds, 2026-07-28). |
| Q3-05 relationship authoring | Complete | Codex | Proposed-only relationship API validates contracts and returns unresolved-dependence warnings without graph mutation; 46 server tests passed |
| Q3-06 shadow simulation | Complete | Codex | Active/candidate paired runs simulate in memory and prove active graph immutability; 48 server tests passed |
| Q3-07 version-bound approval | Complete | Codex | Exact binding hash required; graph edits invalidate pending candidate; approved overrides apply atomically; 50 server tests and 72 kernel tests passed |
| Q3-08 authoring journey | Complete | Codex | Fixture-only target→review→15-factor→elicitation→shadow→approval API journey; 51 server tests and 72 kernel tests passed |
| Gate 3 generalized authoring | Complete | Codex | API supports reviewed, proposed-only, version-bound generalized authoring; fixture-only until live research is consented |
| Define autonomous hybrid GUI goal | Complete | Codex | `GOAL_GUI.md` specifies Build, Run, Edit, and Monitor workflows plus evidence-gated autonomous completion |
| Q4-01 shared UI package | Complete | Codex | `@lns/ui-shared` exports typed lifecycle/client boundaries and deterministic fixtures; both apps consume the shared lifecycle; its current 23-test Vitest 4 suite/type build, zero-vulnerability package audit, 55-server-test catalog/persistence coverage, and both consumer production builds passed on 2026-07-28 |
| Workspace project persistence | Complete | Codex | Typed SQLite project, draft, scenario, and fixture-monitoring metadata persists across restart; stale bases return 409, scenario operations preserve active graph state, and draft creation atomically records the draft plus Refine stage/base version (3 focused tests; 55 server tests, 2026-07-28) |
| Shared Project Home and workspace shell | Complete | Codex | Accessible New/Run/Edit/Monitor actions, project truth cards, lifecycle guard, and visible target/version/freshness/fixture state are implemented in shared components. The lifecycle rail now names and semantically marks the persisted project's current stage. Project Home loads persisted projects plus their target-contract question/horizon and provides explicit operator selection, so existing-model actions bind the chosen project ID; it renders the persisted draft base as non-active, loads the persisted monitoring configuration best-effort, distinguishes fixture/local/saved-live-not-polling state, and counts only unresolved warning/error/alert events. New project persists an Idea-stage workspace before target intake. The canonical app opens through this shared router, with distinct build/run/edit/monitor workspace shells. Server-backed local/live evidence classifications are typed (80 shared tests, shared type build, and canonical production build, 2026-07-28). Full run/edit/monitor controls and receipts remain. |
| Existing-model Run mode | In progress | Codex | Run mode calls the authoritative graph simulation endpoint only for the selected project's approved graph and renders its snapshot ID, graph version, seed, sample count, freshness receipt, per-node mean/median/p05/p95/standard-deviation summaries, and the server-computed multi-seed/multi-sample stability diagnostic with its no-accuracy limitation. Failed snapshots surface their server failure reason and explicitly withhold outcome summaries. SQLite exposes bounded, newest-first read-only snapshot history through a dedicated API endpoint; Run shows receipt ID/version/seed/sample/status/finish time and explicitly says history does not rerun or alter the approved graph. Named scenarios can remain narrative-only, or carry a typed finite parameter override plus the current graph version and target; executable scenarios use the server shadow engine and visibly remain in-memory/non-active/non-approvable. Run also offers a bounded one-at-a-time local finite-difference report for every nonzero active parameter at an explicit fraction, displaying target mean/median deltas, skipped zeros, method, and no-causality/no-accuracy limitations. Successful run metadata persists separately on the project and Project Home shows its snapshot ID; no structural edits occur (8 focused server API tests; 24 focused shared tests; shared type build, 2026-07-28). Prior-run comparison and global sensitivity remain. |
| Existing-model Monitor mode | In progress | Codex | Monitor mode loads/saves project-scoped cadence, freshness threshold, and fixture/local/live mode through the persisted workspace API, returns a save receipt, and explicitly states that fixture mode has no retrieval, local mode requires an external local collector, and live mode does not start polling. Fixture events are explicitly labeled non-live; acknowledgement persists a timestamped receipt without mutating model structure. Inspect now shows an immutable event-detail surface and Branch to edit routes to the draft-only Edit workflow. Re-run action and external polling remain (74 shared tests, shared type build, and 3 focused server tests, 2026-07-28). |
| Existing-model Edit mode | In progress | Codex | Edit mode refuses to start without an approved graph version, otherwise atomically creates a typed server-persisted draft and records the persisted project stage as Refine with that draft base version before any candidate work. It renders the selected approved graph read-only as a target-centered map with filters, keyboard navigation, zoom/pan, textual alternative, selected-factor path tracing, and a contract-backed edge list: stored type, transform, sign, units, coefficient parameters, lag, evidence claims, and state render when persisted; absent legacy values are explicitly `Not recorded`. It reloads and labels durable draft history as non-active. Project-scoped approval now persists the confirmed graph version and Decide lifecycle state after the graph store accepts the exact binding; the active structure remains unchanged until that separate approval. Reversible structural refinement commands and candidate revision deltas remain (123 shared tests; 78 server tests; shared/canonical type builds; 8 E2E tests, 2026-07-28). |
| Candidate revision persistence and comparison | In progress | Codex | The workspace API persists typed, non-active revisions with exact base graph version, numeric parameter overrides, and same-family elicited `DistributionSpec` records, validated active/excluded node- and existing-dependency-state overrides, and kernel-validated proposed new nodes that require human approval. An elicited spec must match its active node family, kernel-normalize to the exact staged numeric override, and cite only known evidence claims; stale bases and unknown references reject, revisions survive restart, and saving one leaves the active graph unchanged. Edit can stage a server-derived same-family distribution through named intuitive inputs, preserve method/as-of/rationale/support/provenance, and map it to the node's documented legacy runtime names for an in-memory comparison. A candidate proposal now hashes the full typed specification plus matching overrides; exact approval atomically persists both on the active node, while ordinary numeric edits clear stale provenance. The returned authoritative graph immediately refreshes the inspector's as-of, support, method, confidence, and evidence fields. Component coverage verifies all six non-quantile input forms; the fixture Monitor → Edit browser flow sends all six through server derivation and saves Normal/Gamma distribution proposals for review. It compares same-base durable revisions as a non-simulating payload delta, including distribution additions/removals/changes; mismatched bases reject. Fitting remains (86 kernel tests; 89 server tests; 121 shared tests; 8 fixture E2E tests; shared/canonical UI builds, 2026-07-28). |
| Structural graph metadata groundwork | In progress | Codex | The active kernel `Graph` now persists a keyed aggregate of active `RelationshipContract` metadata in SQLite and restores it with nodes, graph version, and legacy empty-relationship graphs. Store validation rejects a contract whose ID/key, nodes, or child `depends_on` edge do not match. This is an enabling persistence boundary only: no structural proposal is created, shadow-simulated, approved, or activated by it (2 focused tests; 77 kernel tests, 2026-07-28). |
| Structural coefficient contract groundwork | In progress | Codex | `RelationshipContract` now preserves an optional unique typed finite coefficient-parameter collection alongside its coefficient units, so future structural proposals can bind actual numeric relationship assumptions instead of an importance label. The compatibility path remains metadata-only until the separate structural-proposal validator requires executable coefficients; no draft contract is activated by this change (focused contract test; 78 kernel tests, 2026-07-28). |
| Structural proposal validation foundation | In progress | Codex | A distinct server endpoint creates and persists an immutable, exact-active-version structural relationship-addition proposal with a server binding hash, relationship/dependence validation, and restart coverage. It materializes only a trial graph, requires explicit coefficient parameters for affine additions, rejects duplicate/missing-node edges and nonzero lag as unsupported by the current runtime, and leaves the active graph unchanged. This is not a structural simulation, approval, or application path (2 focused tests; 70 server tests, 2026-07-28). |
| Structural approval foundation | In progress | Codex | The server can approve only its stored structural relationship-addition proposal with the exact binding hash. The graph store rechecks the base version inside one SQLite transaction, writes the child `depends_on`/relationship ID, writes the active relationship contract, maps the declared affine `coefficient` to the child’s exact newly appended `aN` slot, increments graph version, marks freshness stale, and appends change events together. A project-scoped counterpart verifies project graph/version alignment, then returns a confirmed `decide` lifecycle/version after graph acceptance; stale project alignment rejects before graph mutation. Edit uses this endpoint when a project context exists. This remains ordered cross-store synchronization, not a claimed cross-store transaction. Scope is zero-lag existing-node additions; node/removal/state deltas remain (focused server tests; 78 kernel tests; 74 server tests, 2026-07-28). |
| Structural Edit review flow | In progress | Codex | Canonical Edit now distinguishes existing dependency-state changes from proposed relationship additions: the latter selects explicit parent/child factors, requires a finite coefficient, creates a server-issued structural proposal binding, and requires a separate named review acknowledgment before exact-hash approval. It can also invoke the structural shadow endpoint against that stored proposal, render active/candidate summaries in a separate receipt, confirm that the active graph is unchanged, and state that movement is structural impact rather than forecast accuracy. The fixture-intercepted Monitor → Edit browser flow exercises comparison and exact review/approval. Project lifecycle synchronization and node/removal/state-delta approval remain (focused component flow; 96 shared tests; canonical UI build and 6 E2E tests, 2026-07-28). |
| Structural shadow comparison foundation | In progress | Codex | The server reconstructs an exact stored structural relationship-addition proposal as an in-memory trial graph and returns paired active/candidate target summaries with unique run IDs, candidate relationship IDs, no-mutation proof, and no-accuracy limitation. Edit now invokes that endpoint only through the stored proposal ID and renders its own non-activation receipt. Stale bases reject; scope remains zero-lag existing-node additions only (5 focused server tests; 96 shared tests; canonical UI build and 6 E2E tests, 2026-07-28). |
| Structural relationship removal and node retirement | In progress | Codex | A structural proposal may carry exact active relationship IDs to remove, with no browser-local activation. The server materializes a trial by removing the persisted contract and matching child dependency, reindexes the child affine coefficients while preserving `a0`, and validates it before shadow simulation or hash-bound approval. A non-target node can now retire only in that same exact proposal after all of its incident persisted relationships are removed; incomplete metadata or a target retirement rejects. The graph store atomically removes metadata/edges, retires the isolated node, increments version, and writes events; project approval synchronizes the confirmed Decide lifecycle afterward. Edit creates this delta only for metadata-complete staged node exclusions, otherwise explains why it will not create a partial proposal; its receipt names retired nodes and approved staging clears. Fixture browser proof covers retirement → non-mutating comparison → exact project approval. Physical deletion, restoration, and generic state changes remain separate (78 kernel tests; 78 server tests; 100 shared tests; 7 E2E tests, 2026-07-28). |
| Active-versus-candidate comparison | In progress | Codex | Existing-model Edit loads the selected approved graph and stages/removes graph-derived numeric parameter changes as a reversible local candidate set before an in-memory shadow comparison. It renders active/candidate mean and median, a directed factor-to-target affected path, active graph immutability, and the limitation that a shift is structural impact rather than evidence of forecast improvement. A compared set can now persist as a separate version-bound candidate revision or be saved as the review/approval proposal; both paths remain distinct. The interaction is graph-generic and browser-fixture exercised, but does not provide structural graph deltas or model/ensemble comparison (65 shared tests; shared/canonical type builds; 5 E2E tests; 57 server tests, 2026-07-28). |
| Version-bound approval view | In progress | Codex | After a shadow result, Edit can persist the identical override as a server candidate proposal, show its exact graph version and binding hash, require a non-empty operator identity and explicit binding-review acknowledgment, then display the server approval receipt/new graph version and persisted project Decide lifecycle/version. The confirmed project payload immediately updates the workspace lifecycle rail. The graph apply is atomic in the graph store; the project lifecycle write is a subsequent verified workspace-store update. Full proposal diff/path impact and complete run-receipt analysis remain (62 shared tests; 56 server tests; shared/canonical type builds; 5 E2E tests, 2026-07-28). |
| Idea/Vet component foundation | In progress | Codex | Accessible target form distinguishes the Neodymium private-investor retail series, blocks incomplete contracts, collects every authoritative target-contract field with timezone-aware submission values, and has a reusable project-persistence wrapper that reports success/failure without attaching a failed target. In canonical Vet, Pause/Resume works, action-specific discovery entries and planned supply/demand/substitution/recycling/future-use/regime/external-driver categories persist as distinct brief records, and the operator can persist either a local-only preference or an explicit OpenRouter routing-consent receipt that names the limited future scope and states no content was sent. Proceed now does not itself grant provider consent (72 shared tests, shared type build, and 5 E2E tests, 2026-07-28). Provider execution and live research remain. |
| Build target-to-map transition | In progress | Codex | After persisted target intake succeeds, its project stage is durably set to Vet. Proceed now persists Map before showing the operator-triggered candidate-map step. That step loads only the explicit deterministic fixture endpoint and labels the graph fixture/not-live/proposal-only; no candidate becomes active. The canonical browser fixture contains 15 factors and one visible three-hop proposal path. Consented research, live proposal routing, and refinement remain (57 shared tests, shared type build, and 5 E2E tests, 2026-07-28). |
| Canonical viewport smoke and evidence gate | In progress | Codex | Fixture-intercepted Playwright checks pass at `1440x900` and `1280x800` for Project Home → New project → target intake → durable Vet research categories and no-content-sent routing consent → explicit deterministic 30-factor fixture candidate map, with 30 unique rendered coordinates asserted at each viewport. The canonical suite also proves Monitor → read-only event inspection → draft-only Edit branch → durable same-base revision comparison → relationship removal/addition review, plus metadata-complete non-target node retirement → non-mutating structural comparison → exact project approval, and Project Home → Run → authoritative outcome/stability receipt without structural mutation, followed by exact saved ensemble binding acknowledgment and server-receipt display. An axe-backed Project Home check reports no serious/critical violations. `scripts/verify_gui.sh` now fails closed on missing screenshots, fixture-labeled E2E/accessibility receipts, or any P01–P25 proof mapping in `FINAL_GUI_REPORT.md`. These are fixture-only local checks and do not prove live research, live structural simulation, complete accessibility, or complete Run/Edit/Monitor journeys (8 E2E tests, 2026-07-28). |
| Gas execution isolation | In progress | Codex | The Gas adapter imports the shared lifecycle stages and retains preview/dry-run controls, but its live buy and live auto-sell buttons are hard-disabled. Static source verification finds no UI calls with `confirm: true` or `autoSell(true)`, and the gas production build passes (2026-07-28). A separately authorized execution surface is outside this goal. |
| Shared graph and warning foundations | In progress | Codex | The fixture candidate map reached from the canonical Build route renders a target-centered, keyboard-selectable visual graph with deterministic no-default-overlap hop placement, search, state and hop filtering, drag pan, bounded zoom, fit-to-view, selected three-hop relationship trace, non-color selection status, and a full textual alternative. The canonical Map route now renders fixture-supplied limitations in a non-color Warning Center above the graph; a reusable relationship inspector exposes/editably stages type, units, lag, evidence provenance, and state (56 shared tests, shared type build, and 5 E2E tests, 2026-07-28). This remains fixture-only; richer relationship paths, server-backed edits, and evidence linking remain. |
| Graph keyboard accessibility | In progress | Codex | The visual graph group is now keyboard-focusable, carries unique per-instance arrow-key instructions, and has a focused keyboard-navigation test rather than relying on key events bubbling from a selected node. Canonical styles add visible `:focus-visible` treatment and honor `prefers-reduced-motion` by removing transition/animation duration. Broader accessibility coverage remains (focused graph test and shared type build, 2026-07-28). |
| Distribution inspector foundation | In progress | Codex | The reusable inspector renders all eight canonical families with a labeled curve preview, plain-language fit, explicit dispersion/tail behavior, alternatives, limitations, support, canonical parameters, as-of/provenance state, and optional analytic values. In Edit it binds to the selected persisted graph factor’s actual family/parameters and passes recorded unit plus explicit node/spec support bounds through the inspector. The read-only server statistics route normalizes documented legacy parameter names and returns kernel-derived mean/median/mode/variance plus fixed-seed, registry-sampled p05/p50/p95 display quantiles with an explicit method label; the UI displays all returned values without fitting or mutating the node. Missing graph-node as-of/provenance remains explicit. Elicitation and quantitative revision persistence remain (86 kernel tests; 89 server tests; 121 shared tests; shared type build, 2026-07-28). |
| Relationship inspector foundation | In progress | Codex | The reusable inspector is bound read-only to Canonical Edit’s selected persisted active dependency, and the approved-map edge list projects the same persisted contract before a draft begins. Both map stored relationship type, sign, transform, coefficient parameters/units, source/target units, lag, evidence-claim IDs, and state; absent legacy values render as `Not recorded` rather than inferred defaults. Draft-only inspector editing and proposed-contract staging remain separate, and active graph structure still requires exact structural proposal review and approval. The server rejects dangling evidence claims; the fixture-intercepted Monitor → Edit journey asserts the persisted contract’s type and evidence claim in both surfaces. Relationships remain outside generic numeric simulation/approval; physical deletion/restoration and generic state-delta approval remain (123 shared tests; shared/canonical builds; 8 fixture E2E tests, 2026-07-28). |
| Evidence review foundation | In progress | Codex | The canonical Vet stage mounts a review drawer with source/conflict context and explicit Include/Exclude decisions. Edit derives claim IDs from persisted node distribution specifications and active relationship contracts, filters the same target review to those graph-linked claims, and narrows to either the selected approved factor or selected persisted relationship’s claims. The workspace memoizes its shadow-comparison adapter so contextual selection cannot reset candidate draft values. The canonical browser flow proves the graph-linked relationship claim surface. Live source capture remains (124 shared tests, shared/canonical type builds, and 8 E2E tests, 2026-07-28). |
| Fixture workspace composition | Complete (fixture-only) | Codex | Shared fixture workspace composes target intake, graph, and warning surfaces inside the workspace shell (focused composition test and type build, 2026-07-28). It is not a substitute for the required persisted/live workflow. |
| Gate 4 visual workspace | Pending | Unassigned | Depends on Gate 3 API/contracts |
| Gate 5 Neodymium acceptance | Pending | Unassigned | Depends on Gates 1–4 |
| Gate 6 gas adapter | Pending | Unassigned | Depends on shared workspace |
| Gate 7 CI/handoff | Pending | Unassigned | Final gate |

## Decision Links

- Product/domain decisions: `DECISIONS.md` D-001 through D-005
- Modeling decisions: D-006 through D-010, D-016, D-017
- Approval/research decisions: D-011 through D-015
- UI/demo decisions: D-018, D-019
- Workflow/autonomy/ensemble decisions: D-021 through D-024

## Current Milestone

**Gate 4 — Prediction Workspace**

Gate 3 exited on 2026-07-28: fixture-tested target intake, evidence review, broad candidate proposal, distribution elicitation, relationship validation, shadow simulation, and hash-bound approval are verified. The next exit is the shared visual workspace that makes this reviewable logic usable.

## Next Actions

1. Add fitting/revision inputs where evidence supports them without duplicating registry math in the browser.
2. Add visual curve editing/review controls that preserve the server-owned distribution contract.
3. Keep fixture proposals clearly labeled until live research is consented and completed.

## Blockers

No blocker to Gate 0 contract work.

Potential future blocker:

- A reproducible and legally usable Neodymium historical series is not yet confirmed. This blocks claims of historical multi-hop lift, not contract/kernel/UI development.

## Questions for User

None currently blocking. Pending technical decisions have safe defaults in `DECISIONS.md`.
